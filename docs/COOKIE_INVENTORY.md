# Cookie inventory

| Name / category | Type | Purpose | Duration | Consent |
| --- | --- | --- | --- | --- |
| Auth.js session cookie (`authjs.session-token` / `__Secure-authjs.session-token`) | Strictly necessary | Authenticated session | Session / up to 7 days | Not required (necessary) |
| Auth.js CSRF / callback cookies | Strictly necessary | Auth flow security | Short-lived | Not required |

## Optional technologies

None currently loaded. No cookie banner is shown while only necessary cookies are used.

When analytics or marketing is added later:

- Block until explicit choice
- Equal prominence: Alles accepteren / Alleen noodzakelijk / Instellingen
- Store categories, policy version, timestamp
